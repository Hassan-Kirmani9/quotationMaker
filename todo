1) do we need getAll API or just pagination API or both or one
2) filter by userid or tenant
3) add userid and tenant on each models and endpoints
4) groupby pages by name and only those render which have permissions array from backend