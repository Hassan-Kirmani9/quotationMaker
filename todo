1) never getAll, just pagination => done
2) filter by userid => done
3) add userid and tenant on each models and endpoints => done
7) added mongoose-delete plugin => done
9) using node v22 => done


6) validity in
8) check mongoose-pagingate plugin if you think its of any use - first tell me
5) for get quoation, create 2 seperate endpoints one to get quotation by id and one to get quotationItems by id
4) groupby pages by name and only those render which have permissions array from backend
10) validity
